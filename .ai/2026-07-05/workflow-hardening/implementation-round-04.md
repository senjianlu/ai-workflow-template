---
task: workflow-hardening
round: 04
date: 2026-07-05
---

# 实现记录:第 04 轮

> 本轮超出工作流默认的 3 轮修复上限,依据第 03 轮评审后用户的明确授权
> ("授权第 4 轮修复")进行,授权时间 2026-07-05。

## 本轮改动

| 文件 | 改动摘要 |
|---|---|
| `.ai-workflow/scripts/review.sh` | ① `workspace_hash()` 未跟踪文件改为 NUL 定界记账(`类型\0路径\0载荷\0`):符号链接载荷为 `readlink` 直接写入流(不经命令替换),普通文件载荷为 `shasum < 文件` 的纯内容哈希;diff 段与未跟踪段各自先哈希再合并,杜绝跨段拼接歧义 ② `$raw` 解析前增加存在性检查,codex 退出 0 但未产出输出文件时显式 exit 3 |

## 修复对照

| 评审问题编号 | 严重度 | 修复方式 |
|---|---|---|
| R-01(第 03 轮) | major | 序列化改为 NUL 定界:` -> ` 分隔符退场,路径与链接目标间不存在可碰撞的字面歧义;`readlink` 输出直写流,规避命令替换吞尾部换行;文件名内含换行等特殊字节亦被 NUL 框定。新增 TC-18 用评审者给出的碰撞对(`src/link -> "x -> y"` 换成 `"src/link -> x" -> y`)验证:修复前两状态哈希相同(即第 03 轮 R-01 的复现),修复后 rc=3 |
| R-02(第 03 轮) | major | codex 成功但 `$raw` 缺失时,输出"未产出评审输出文件"并 exit 3,不再落入 `sed` 在 `set -e` 下的隐式退出码 1。新增 TC-17 验证:stub 退出 0 且不写文件 → rc=3、无评审文件生成 |

## 测试结果

stub codex 增加 `no-output`(退出 0 不写文件)与 `swap-ambiguous`(把预置的 `src/link -> "x -> y"` 符号链接替换为 `"src/link -> x" -> y`)两种行为;TC-02〜TC-18 全量复跑:

| 编号 | 结果 | 证据 |
|---|---|---|
| TC-02〜TC-16 | 通过(回归) | 驱动输出 15 条 `PASS`,判定条件与第 03 轮一致 |
| TC-17 | 通过 | `TC-17 PASS (rc=3,codex 无输出被识别为执行异常而非 fail)`,且无 review-round 文件生成 |
| TC-18 | 通过 | `TC-18 PASS (rc=3,旧序列化下碰撞的两个状态现可区分)`,stderr 报"改动了工作区" |
| 语法检查 | 通过 | `bash -n` review.sh 无输出 |
| settings.json 复核 | 通过 | 全量自测(17 项)后 `git diff HEAD --quiet -- .claude/settings.json` 成立,输出"settings.json 仍无 diff" |

合计 17/17 通过。

## 与方案的偏差

- 本轮为评审修复轮,改动严格限于第 03 轮 R-01、R-02,无评审范围外改动。
- 轮次超出 plan 隐含的 3 轮上限,授权来源与经过见本文件顶部说明。
