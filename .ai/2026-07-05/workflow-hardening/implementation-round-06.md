---
task: workflow-hardening
round: 06
date: 2026-07-05
---

# 实现记录:第 06 轮

> 本轮依据第 05 轮评审后用户的明确授权("授权第 6 轮修这一处")执行。

## 本轮改动

| 文件 | 改动摘要 |
|---|---|
| `.ai-workflow/scripts/review.sh` | 同轮唯一性由原子锁保证:任务目录下 `mkdir .review-lock`(POSIX 原子操作)罩住"检查-评审-发布"全程,并发第二实例直接以 exit 3 拒绝并提示;锁经 `trap EXIT` 与副本一并清理,陈旧锁(如 kill -9 遗留)的清理方式写在拒绝提示中。锁为临时空目录,对 git 状态与完整性哈希均不可见 |

## 修复对照

| 评审问题编号 | 严重度 | 修复方式 |
|---|---|---|
| R-01(第 05 轮) | major | "检查与写入不具原子性"的竞态窗口由互斥锁消除:检查同轮评审文件是否存在、执行评审、发布结论文件全部发生在持锁区间内,两个并发实例不可能都通过检查。TC-22(真实并发:慢速评审持锁期间第二实例被拒,同轮最终仅 1 份评审文件,锁清理干净)与 TC-23(陈旧锁被拒绝并给出清理指引)验证 |

## 测试结果

stub codex 增加 `slow-pass`(sleep 2 后写 pass,用于制造确定性的并发窗口);TC-02〜TC-23 全量复跑:

| 编号 | 结果 | 证据 |
|---|---|---|
| TC-02〜TC-21 | 通过(回归) | 驱动输出 20 条 `PASS`,判定条件与第 05 轮一致 |
| TC-22 | 通过 | `TC-22 PASS (并发第二实例被锁拒绝,同轮仅 1 份评审文件,锁已清理)`:实例 A(slow-pass)持锁期间实例 B 启动 → B rc=3 且 stderr 含"已有评审在进行中";A 正常完成 rc=0;终态 review-round-01-*.md 恰 1 份;`.review-lock` 不存在 |
| TC-23 | 通过 | `TC-23 PASS (rc=3,陈旧锁被识别且提示清理方式)` |
| 语法检查 | 通过 | `bash -n` review.sh 无输出 |
| settings.json 复核 | 通过 | 全量自测后 `git diff HEAD --quiet -- .claude/settings.json` 成立 |

合计 22/22 通过。

## 与方案的偏差

- 本轮为评审修复轮,改动严格限于第 05 轮 R-01,无评审范围外改动。
