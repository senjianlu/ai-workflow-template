# 评审:第 04 轮

## 问题清单
- [major] R-01 遗留的原始评审输出可被下一次运行误用
  - 详情:.ai-workflow/scripts/review.sh:60 / 首次评审因工作区完整性失败退出时会遗留含 `VERDICT: pass` 的 `$raw`；恢复工作区后重跑，若 Codex 退出 0 但未写输出，脚本会接受旧文件并返回 0 / 调用 Codex 前删除旧 `$raw`，或为每次运行使用唯一临时文件并验证本轮确实生成
- [major] R-02 未跟踪文件的可执行位变更未纳入完整性哈希
  - 详情:.ai-workflow/scripts/review.sh:51 / 普通文件仅记录路径和内容哈希；实测 Codex 对未跟踪文件执行 `chmod +x` 后脚本仍返回 0，而该权限会被 Git 提交并改变文件行为 / 将未跟踪文件的 Git 相关模式信息纳入序列化，并增加权限位变更测试

## 总评
TC-01～TC-18、语法检查及 settings.json 复核均实际通过，第 03 轮问题已修复。但完整性校验仍存在两个可复现的绕过路径，必须修复。

VERDICT: fail