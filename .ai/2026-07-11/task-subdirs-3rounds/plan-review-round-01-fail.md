# Plan 评审:第 01 轮

## 问题清单
- [major] R-01 TC-04 无法证明第 3 轮评审被放行，核心边界路径未被真实覆盖
  - 详情:定位：测试用例 TC-04 与 `.ai-workflow/scripts/plan-review.sh` 主流程。脚本在轮次计数之前就执行 `command -v codex`；当前置条件为“codex 不可用”时，脚本会立即 exit 3，根本不会经过 `count >= 3` 的轮次闸。因此该用例无法验证已有 2 轮时会放行第 3 轮。建议在临时 PATH 中放置可记录调用并返回受控结果的假 codex，分别验证已有 2 轮时确实调用一次、已有 3 轮时完全不调用并输出上限提示；同时校验对应退出码。
- [major] R-02 方案夹带了与既定两项目标无关的全局中文沟通硬规则，范围自相矛盾
  - 详情:定位：背景与目标声明任务仅包含“评审上限 2→3”和“新增 evidence/assets”，但改动范围及实现方案又要求在 `CLAUDE.md` 新增“与用户沟通一律使用简体中文”。这是会长期改变所有 Claude Code 用户交互行为的全局政策，不是前两项调整的必要组成，也没有背景、决策依据或风险分析。按当前方案实施后若该规则并非明确需求将需要回退，且会把一个独立政策变更混入本任务。建议从本方案删除该改动及 TC-06；若确属用户要求，应将其列为第三个明确目标并补充适用范围与决策依据。

## 总评
轮次阈值和任务子目录的主体设计与现有 rawf 工作流基本兼容，新增目录也能被现有 `.ai/` 放行及任务目录整体提交规则覆盖。但核心放行边界的测试因脚本前置检查顺序而无效，同时方案存在一项未纳入目标的全局政策变更，因此需修订后重评。

VERDICT: fail
