# Plan 评审:第 01 轮

## 问题清单
- [plan-blocker] R-01 方案遗漏了实现者证据契约的同步修改，且与现有 rawf-implement 指令直接冲突
  - 详情:plan.md:31-33、45-47、58、67 明确不修改各 SKILL.md，却要求边界外用例必须在 evidence/ 中有原始输出，缺失即按虚报处理。现有 .claude/skills/rawf-implement/SKILL.md:27-30 只要求在实现记录中保存“实际命令 + 关键输出”，仅将体量较大的完整日志放入 evidence/，并仍声明“评审者会重跑验证”。因此合规实现也可能因没有 evidence/ 原始输出而被误判 blocker，同时 TC-04 会发现方案刻意保留的矛盾表述。应将 rawf-implement/SKILL.md 纳入范围：明确边界外用例必须在评审前保存何种原始证据，并将“评审者会重跑”改为按动作边界分别重跑或核验证据；TC-04 应覆盖这两项契约。
- [major] R-02 以测试运行器名称区分单元测试无法保证动作白名单，仍可能运行集成测试或长驻任务
  - 详情:plan.md:17-21、39-44 将 vitest run / pytest 整体列为允许动作，但仓库约定中后端集成测试也并入 tests/，pytest 命令本身不代表单元测试；Vitest 套件同样可能包含启动服务、访问网络或不自终止的用例。直接运行宽泛的 pytest/vitest 命令仍可能违反“只允许运行自终止单元测试、禁止服务和长驻进程”的核心验收目标，而现有 TC-01～TC-05 只检查文本是否出现，未覆盖该边界。方案应按测试用例性质和明确的项目命令/marker/path 判定是否可运行，并规定超时或中止策略；增加覆盖混合单元/集成测试套件及疑似长驻命令的验收用例。

## 总评
方案选择 prompt 层动作边界并保留副本隔离总体可行，但实现者与评审者之间的证据契约尚未闭环，而且命令级白名单不能真实限制到单元测试。按 plan-review 规则，存在 plan-blocker 和 major，需修订方案后重评。

VERDICT: fail
