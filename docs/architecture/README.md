# 架构总览

<!-- TEMPLATE: 本文件是 docs/architecture/ 的汇总和导航文件。
AI 编码工具更新架构文档时,先维护本页的目录地图与系统形态;更详细的
主题信息下沉到同目录其他 Markdown 文件中,并从下表路由过去。存量项目
迁移时,老 CLAUDE.md 中的现状/硬约束描述搬到本目录。 -->

本目录组织:

| 文档 | 内容 |
|---|---|
| `README.md`(本文) | 目录地图 + 系统形态 |

> 上表是推荐拆分。项目可按实际形态增删文件,但 `README.md` 始终只做
> 汇总和导航,不要重新膨胀成单页大文档。

## 系统形态

<!-- TEMPLATE: 用少量段落说明仓库/应用/部署单元的当前形态。
例如:单体还是 monorepo,有哪些 apps/packages,哪些进程角色共用镜像,
静态产物由谁托管。细节路由到上表对应文件。 -->

| 位置 | 内容 |
|---|---|
| `deploy/` | 镜像、compose、反向代理、外部依赖编排 |

## 回写规则

- README 只维护目录地图、系统形态和跨主题入口。
- 某一主题超过本页简述体量时,在同目录新增或更新对应文件,再把入口写回
  上方表格。
- 影响原因与取舍的长期决策写入 `../decisions/`;当前形态与运行规则写入
  本目录。
